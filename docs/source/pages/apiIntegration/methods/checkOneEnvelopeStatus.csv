**URL**,
Method,GET
Request URL,``/api/v1/envelope/{uuid}``
"uuid(path, require)",UUID of envelope to retrieve
**Headers**,
Authorization(require),"Bearer “token” – token, which get after `Authorization <methods/authorization.html>`__      "
Mailbox(require),mailboxUuid: mailbox id
Content-Type,application/json
